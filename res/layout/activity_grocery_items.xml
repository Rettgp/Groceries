<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context=".GroceryItems" >
    
    <TextView
        android:id="@+id/groceryListHeader"
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:background="@drawable/categoryborder"
        android:gravity="center"
        android:text="GroceryList"
        android:textStyle="bold"
        android:textColor="#FFFFFF"
        android:textSize="40dp"/>
    
    <ListView
        android:id="@+id/itemsView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignLeft="@+id/groceryListHeader"
        android:layout_below="@+id/groceryListHeader" />
    
    <Button
        android:id="@+id/buttonAddCategory"
        style="?android:attr/buttonStyleSmall"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBaseline="@+id/groceryListHeader"
        android:layout_alignBottom="@+id/groceryListHeader"
        android:layout_alignRight="@+id/groceryListHeader"
        android:background="@drawable/circle"
        android:minHeight="30dp"
        android:minWidth="50dp"
        android:onClick="onItemAddClicked"
        android:text="+"
        android:textColor="#FFFFFF"
        android:textSize="40dp"
        android:textStyle="bold" />   

</RelativeLayout>
